// const AlertMessages = ({messages}) => {
//   return (
//     <>
//         {/* <!-- Flash Message for Errors --> */}
//         {messages.errors && messages.errors.map((el, i) =>
//             <div key={i} className="alert alert-danger">{ el.message }</div>
//         )}
//         {messages.success && messages.success.map((el, i) =>
//             <div key={i} className="alert alert-success">{ el.message }</div>
//         )}
//         { messages.info
//             ? messages.info.map((el, i) =>
//                 <div className="alert alert-info">{ el.message }</div>
//             )
//             : null
//         }
//     </>
//   )
// }

// export default AlertMessages
